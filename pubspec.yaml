name: navegacao_entre_telas
description: "A new Flutter project."

publish_to: 'none' # Remova esta linha se quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/cod.png
    - assets/images/ODS-12-2.webp
    - assets/images/Fieb.webp
    - assets/images/th.webp
    - assets/images/fiebof.png